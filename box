import 'package:flutter/material.dart';
class Listbox extends StatefulWidget {
  @override
  _ListboxState createState() => _ListboxState();
}

class _ListboxState extends State<Listbox> {
  @override
  Widget build(BuildContext context) {
        return new Scaffold(
          body:new ListView(

            children: <Widget>[
             new SizedBox(height: 20),
             new  Container(alignment:Alignment.center, child: new Text('عن ماذا تبحث؟',style: new TextStyle(fontSize:25,),),),
             new SizedBox(height: 20),
              new Row(
                crossAxisAlignment: CrossAxisAlignment.center,
                children: <Widget>[
                 new IconButton(
                    icon: new Image.asset('assets/icon/cocktail.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
    
                 new IconButton(
                    icon: new Image.asset('assets/icon/Eyeglas.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
                ],
              ),
              new Row(
                crossAxisAlignment: CrossAxisAlignment.center,
                children: <Widget>[
              new IconButton(
                icon: new Image.asset('assets/icon/Food.png'),
                iconSize: 160,
                onPressed: () {},
                ),
    
              new IconButton(
                icon: new Image.asset('assets/icon/gym.png'),
                iconSize: 160,
                onPressed: () {},
                ),
                  ],
              ),
              new Row(
                children: <Widget>[
                 new IconButton(
                    icon: new  Image.asset('assets/icon/love.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
    
                 new IconButton(
                    icon: new Image.asset('assets/icon/Online.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
                ],
              ),
              new Row(
                children: <Widget>[
                 new IconButton(
                    icon:new Image.asset('assets/icon/Settings.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
    
                 new IconButton(
                    icon: new Image.asset('assets/icon/Shop.png'),
                    iconSize: 160,
                    onPressed: () {},
                   ),
                  
                ],
             ),
        ],       
      ),
      floatingActionButton: FloatingActionButton.extended(
       onPressed:(){},
       tooltip: 'تخطي',
       icon: Icon(Icons.navigate_next),

       label: Text("تخطي",style:TextStyle(fontSize:25,),),
      ),
    );
  }
}
